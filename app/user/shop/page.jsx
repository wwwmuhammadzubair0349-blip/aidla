"use client";
// app/user/shop/page.jsx
// No changes needed — no router, no supabase, no imports to update.
// Only "use client" added (required for any interactive page in App Router).

export default function Shop() {
  return (
    <div>
      <h2>Shop</h2>
      <p>Coming soon...</p>
    </div>
  );
}